# FILL REGRESSION 04 -- num_regions:3 + chain + rune_source=pool: create_items must seat the
# region locks count-neutral (no overflow, no spill of locks to the start inventory).
# Guards: er-rune-skip-injectable-room.
#   (Originally guarded great_runes_present injection; that option was removed in the Part 2
#    consolidation. This still exercises the distinct pool+chain fill path. Filename is legacy.)
name: fillreg04
description: num_regions 3 + chain + pool rune-source lock-placement guard
game: EldenRing
EldenRing:
  enable_dlc: false
  world_logic: region_lock
  ending_condition: capital
  num_regions: 3
  region_access: warp
  location_pool: all
  pool_builder: true
  missable_location_behavior: allow_useful
  excluded_location_behavior: allow_useful
  early_leveling: true
  torrent_start: on
  accessibility: minimal
  progression_balancing: 50
