# Short Solo -- A 3-4 hour Capital run: four regions, then Morgott.
# Generated by tools/dump_options_metadata.py from Archipelago/worlds/eldenring/options.py
# (do not hand-edit; re-run the dump to regenerate)

name: Player
description: ER options wizard preset - Short Solo
game: EldenRing

EldenRing:
  ending_condition: "capital"
  # ^ Ending Condition (default: final_boss)
  num_regions: 4
  # ^ Num Regions (Capital run) (default: 0)
  early_leveling: true
  # ^ Early Leveling (skip Melina) (default: False)
  excluded_location_behavior: "allow_useful"
  # ^ Excluded Locations Behavior (default: forbid_useful)
  missable_location_behavior: "allow_useful"
  # ^ Missable Locations Behavior (default: forbid_useful)
  location_pool: "trimmed"
  # ^ Location Pool Size (default: all)
