Deliberately holds no *.xml: this is the "the Enemy dir exists but was never unpacked" fixture.
A non-xml file is here only so git tracks the directory (and so check_integrity.py never sees a
zero-byte placeholder).
