name: "first_run"
description: "generated by the ER options wizard 2026-07-03 (preset: First Run)"
game: EldenRing

EldenRing:
  early_leveling: true   # Early Leveling (skip Melina) (default: false)
  auto_upgrade: true   # Auto-Upgrade (default: false)
  excluded_location_behavior: "allow_useful"   # Excluded Locations Behavior (default: forbid_useful)
  missable_location_behavior: "allow_useful"   # Missable Locations Behavior (default: forbid_useful)
  no_weapon_requirements: true   # No Weapon Requirements (default: false)
  location_pool: "trimmed"   # Location Pool Size (default: all)
