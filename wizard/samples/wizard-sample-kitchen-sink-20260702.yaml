name: "Kitchen: Sink"
description: "generated by the ER options wizard 2026-07-02 (preset: Full DLC Journey)"
game: EldenRing

EldenRing:
  world_logic: "region_lock_bosses"   # World Logic (default: region_lock)
  extra_region_locks: ["godrick", "snowfield"]   # Extra Region Locks (default: [])
  enable_dlc: true   # Enable DLC (default: false)
  dlc_timing: "late"   # DLC Timing (default: off)
  death_link: true   # Death Link (default: false)
  progressive_items: ["flasks", "physick"]   # Progressive Items (default: [])
  exclude_locations: ["blizzard"]   # ERExcludeLocations (default: [])
