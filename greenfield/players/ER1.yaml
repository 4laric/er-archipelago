# Vanilla + Death Link -- The base game, untouched, with shared deaths.
# Generated by tools/dump_options_metadata.py from greenfield/eldenring core.py -> GFOptions (imported, upstream AP 0.6.7)
# (do not hand-edit; re-run the dump to regenerate)

name: Player
description: ER options wizard preset - Vanilla + Death Link
game: Elden Ring

Elden Ring:
  vanilla_placement: "all"
  # ^ Vanilla Placement (default: off)
  death_link: true
  # ^ Death Link (default: False)