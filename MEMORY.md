<!-- ============================================================================
  ⚠️  THIS IS NOT THE MEMORY STORE — DO NOT EDIT THIS FILE.

  This is a READ-ONLY MIRROR, auto-exported from Claude's Cowork memory.
  Edits made here are IGNORED and will be overwritten on the next export.

  The single source of truth is Claude's internal Cowork memory, which is the
  copy that actually loads into context each session. To change anything here,
  ask Claude to update its memory (it edits the internal store, then re-exports
  this mirror). This file exists only so the index is visible/version-controlled
  in the repo and as the basis for the weekly kanban rebuild.

  Last exported: 2026-06-30
============================================================================ -->

# Memory index (mirror)

> Live project board (kanban, prioritized) at C:\Users\alari\Documents\er-archipelago\er-archipelago-kanban.html — rebuilt weekly from this index. Keep statuses in the canonical store current so the rebuild stays accurate.

## Working style & environment (durable)
- Memory store is canonical (Cowork), repo Memory.md is a mirror — the internal Cowork memory dir is the single source of truth (auto-loads each session); THIS repo file is a read-only export — never edit it
- Trust Alaric's cleared layers — when he says a layer works / "assume X deployed", that's GROUND TRUTH; don't burn his cycles re-verifying; doubt my OWN inherited assumptions first
- Builds solo, undershoots outreach — does deep solo build brilliantly but stops short of community/upstream outreach; surface that angle EARLY
- Verify before external/PR — 🛑 verify ground truth on disk before external messages or PR/commit cmds; NEVER `git add -A` in SoulsRandomizers (root has provisioned game data) — whitelist `git add -A RandomizerCommon EldenRingRandomizer` + check `git diff --cached --stat`
- Don't use AskUserQuestion — slow, has killed long sessions; ask clarifying Qs inline as prose
- Verify files before naming — confirm files/commands exist; always give full paths
- Don't assert patch-state from mount — verify on Windows; trust only POSITIVE finds
- Local gen from source not apworld — gen runs from worlds/eldenring/ SOURCE; verify patch-state from source, not zip/mount
- ER patches run on Windows — ⛔ NEVER edit __init__.py/apworld directly; write patch_*.py for Alaric to run on Windows; verify via Read
- CRLF edit truncation — Edit tool truncates CRLF source; patch via Python + verify on disk
- Bash read truncation — sandbox bash can silently truncate large-file reads; verify length/braces
- ER mount write truncation — Write/Edit truncates/null-bytes larger mount files; build in /tmp, cp, verify bytes
- Mount error → hand off patch — mount error on a patch → stop, hand to Alaric; don't re-write to "fix"
- Sandbox mount no-unlink — mount blocks rm/unlink (rename+overwrite OK); drive git via /tmp index, commit on Windows
- ER sandbox git sync — sync repos via GitHub HTTPS+PAT (SSH blocked); re-paste token each session
- SR generated files untracked — root /event /msg /script /regulation.bin + *.bak* are build outputs (untrack+ignore); reg_*.bin/regulation_dlc.bin/dist*/ are source — KEEP
- Parallel shared-doc clobber — don't co-edit shared docs in parallel; per-track files, reconcile serially
- Always timestamp dump files — every dump gets a timestamp-in-filename version, even one-offs
- Don't trust un-timestamped files — ground truth = generated spoiler; gendiag dumps resolved options
- AP zip seedname≠seed — AP_<name>.zip filename ≠ spoiler's internal Seed:; match runs by reading inside the zip
- build loop UX fixes — -Generate hangs on gen error (AP_NONINTERACTIVE); baker headless arg
- Repo cleanup tool — cleanup_repo.ps1 (dry-run default) purges cruft + archives finished one-offs

## Project structure, build & infra (reference)
- ER offline artifacts & tooling — elden_ring_artifacts/ has eldenring.exe, Hexinton CE table (AOBs), Smithbox param CSVs, regulation.bin, WitchyBND.exe; resolve RE statically before touching the live game (KRAK assets still need Windows)
- ER project structure — THREE repos (apworld, randomizer, runtime client)
- SR build env — ⚠️ which tree builds FLIPPED: as of 2026-06-28 build from er-archipelago/SoulsRandomizers via default `build.ps1 -Randomizer` (NO -ApEnable); the -ApEnable scratch C:\er-ap-build failed on missing diste\Defs; verify dep state per tree
- ER dev environment — gen/test/build commands; sandbox can't run AP (needs Python 3.11+)
- ER multiworld test runbook — 2-slot gen+serve+bake vs Rust client; ⚠️ bake picks slot from FIRST yaml alpha; wrap build.ps1 w/ HASHTABLE splat (positional [int]GenRetries); Rust swap LAST not -All; run-test.ps1
- ER -Clean -All convention — default full clean build on real change; kill stale :38281 first
- ER randomizer build recipe — deps/pins/patches to build SoulsRandomizers Release
- ER client build speed — slow client build = Core.h→apclient header soup; /MP + LTCG-off + -NoClient
- ER apworld packaging — build.ps1 -Apworld zips worlds/eldenring→eldenring.apworld
- ER FromSoft AP ecosystem — community/contacts REFERENCE ONLY (AP Discord ER chan, nex3, lBedrockl apworld origin, DS1/Sekiro precedent); ⚡ NO upstream path — matt + fswap both out 2026-06-29, ER ships standalone
- DS1 AP (DSAP) recon — tathxo/DSAP playable beta; built on ArsonAssassin's MIT Archipelago.Core runtime memory-edit framework = same shape as Alaric's Rust client, sidesteps Matt licensing; primitives map 1:1 (FunctionHook≈detour grant)
- Nightreign AP = Hades pattern — de-escalating-scaling-as-checks idea = port of Polycosmos "Reverse Heat"; distilled location/goal/winnability template + Nightreign mapping
- ER yaml overrides -Enemies flag — yaml enemy_rando overrides build.ps1 -Enemies
- ER deploy hygiene gap — build.ps1 deploy overlays w/o purge; stale MSBs leak; fix=tools\deploy_hygiene.ps1
- ER event flag validity — flags GROUP-allocated; invented ids no-op; probe set→readback; 62xxx/71-76xxx valid
- ER ?EventTextForMap? failsafe — in-game ?Tag? = regulation.bin vs deployed msg mismatch; fix=full restart + clean redeploy
- ER sphere dump tooling — reachable-checks-per-sphere via ER_DUMP_SPHERES; ER emits spoilers
- ER location id map — canonical AP location id↔name via exec'ing locations.py w/ stubbed imports
- ER filldiag tool — ER_DUMP_FILL dumps adv/priority/slack/per-region; gen_sweep -Seeds reproduces
- ER apworld key mismatch — MASTER LOG: bake pipeline debugging; dev-loop tools + ap_* diags
- ER seed-archive convention — park/resume a playable seed across rebakes; seeds-archive/ + .ps1
- ER baker reads slotdata live — baker pulls options from :38281 slot_data, not yaml/zip; stale binary silently applies old
- ER fill-regression suite — gen-test fill-regression-yamls + run_fill_regression.ps1 gate

## 🔴 Broken in-game (active)
- ER deathlink apconfig tag gap — DeathLink dead BOTH ways: apconfig.json has no death_link → tag never advertised at Connect (P1, NEW 2026-06-28); quick-fix hand-add to game apconfig; durable=baker writes it OR client ConnectUpdate. Outgoing also needs read_local_death HP+0x138 RE validation
- ✅ ER AP icon override — FIXED 2026-06-30: real icon source is the 01_common SPRITE SHEET (SB_Icon_00 cell @2132,1148), NOT 00_solo MENU_Knowledge. Fix = `build_ap_icon.py --icon01`. (resolved — drop from this list on next kanban rebuild)
- ER spirit bell flag — Spirit Calling Bell still unusable in-game despite obtained flag 60110 (P1)
- ER DLC lock notifications — notifs don't fire for DLC region locks; likely b/c many DLC locks share one map piece (m61) (P2, NEW 2026-06-23)

## 🟠 Next up — ready to build/bake (patch written)
- ER kick kill-keep-runes — ONLY outstanding region-lock bug: break-lock kills player but STILL loses runes; patch flips Should Receive Runes→TRUE, baker rebuild+rebake only (P1)
- ER bake slotdata timeout — sync GetSlotData times out; fix=request at login; NOT done, rebuild+rebake (P1)
- ER natural-key triggers impl — 4 patches (Mountaintops/Snowfield/Altus blooms + Spelunker torches + Rold 400001); apply+build pending
- ER Mountaintops lock design — DECISION Option B (keep Rold, gate bloom on Rold+Morgott dead 11000800); ships with triggers
- ER Snowfield lock split — opt-in snowfield lock (101/130 checks); patch written, needs run+gen-test
- ER chokepoint locks impl — gate legacy back-half on choke-boss drop (Godskin Duo+Loretta); needs build+gen-test
- ER DLC catacombs lock — opt-in dlc_catacombs bundles Fog Rift+Belurat Gaol (41 checks); needs gen-test
- ER dlc_only chain — Phase 1 (messmer) generates; Phase 2 full-tree patch written
- ER hint full region names — extend_hint_information spells out parent_region.name; NOT implemented yet, needs run+gen-test
- ER furnace pot injection — furnace_pot_count seeds Hefty Furnace Pots into DLC seeds; written, needs run
- ER furnace golem scaling — c5170 golems opt out of tier-1 fallback; patch needs Windows build+bake+walkup
- ER Messmer kindling cap — baker raises goods 2008021 maxNum to 99; uncommitted
- ER soft-consumable cleanup — soft_consumable_shop + derandomize_gurranq; patch+spec, shop side needs baker
- ER dlc_gear_curation — swap worst base gear 1:1 for best S/A DLC gear; needs gen-test
- ER trimmed curation impl — curation.py cut lists + _in_location_pool; needs gen-test
- ER trimmed forbid_useful shortage — trimmed+forbid_useful fails 'not enough filler'; fix=both allow_useful
- ER trimmed lock-spill — trimmed dlc_only spills all region locks to start; location_pool=all fixes it

## 🔵 In progress (partial)
- ER pure-runtime model — ⭐ SoulsRandomizers-FREE runtime: vanilla game + apworld + MIT Rust client + static flag table (4,759 loc); loop closed in-game 2026-06-29 (solo/base-game/1 pickup); routes around matt licensing; breadth/DLC/multiworld pending; docs PURE-RUNTIME-{NEXT,ARCHITECTURE}.md
- ER client test-coverage plan — tests-FIRST then converge ER Rust client onto fswap `shared`; mock the game INTERFACE not the process; pure logic→new `er-logic` crate, game I/O→GameHook+FakeGame; PR-A first cut delivered (9 tests)
- ER Rust client Phase-1 validated — C++→Rust port spike reached EquipParamGoods in-game (rowCount 3795/firstRowId 0); Strategy-B assumption proven; gotchas (stable vs retour-nightly, in-world param gate, crash breadcrumbs); next=Phase-3 GenericDetour pickup
- ER Rust client Phase-4 net — AP networking on archipelago_rs 2.1.1 (branch port-gap-map); net.rs/grant.rs + detour grant_full_id; COMPILES GREEN Windows `--features net`; in-game test pending
- ER Rust client Phase-5 COMPLETE — full Phase 5 (region-lock, progressive, deathlink, auto_upgrade/scadu) BUILT GREEN + IN-GAME TESTED 2026-06-24; basis for DRAFT PR into fswap clients repo (adds eldenring-archipelago + er-codec/er-semver); converge onto `shared` crate next
- ER completion scaling — geographic pass PLAYTEST-VALIDATED; sphere-basis bridge wired but resolver coverage partial, not bake-validated (P1)
- ER boss scaling tiers — 23 DLC bosses model-keyed; gen-tested + scaling-diag validated; only remaining = playtest for feel (P2)
- ER random start region — apworld+baker+client done & bake-confirmed (lands Caelid); in-game latch pending
- ER random-start Roundtable hub — re-root hub to Roundtable; apworld re-root pending (graph Limgrave-rooted)
- ER notify item source — 'X from Y': data threaded through queue; pending build
- ER auto_equip spec — apworld half done; gap=client equipItem stub (RE ChrAsm equip fn)
- ER yaml comprehension layer — annotated ref + er_yaml_lint.py built; pregen wiring + docstring discrepancy open
- ER poptracker dlc_only — auto-clears non-DLC + Land of Shadow map; map needs DLC re-dump
- ER DLC area_ids — 4 interior DLC regions wired; overworld m61 tiles need in-game place-name capture
- ER enemy-drop tagging — tag enemy-dropped checks via `enemy cXXXX_YYYY`; pure enemy-lot vs event-enemy

## 🟣 Backlog — specs / ideas
- nex3 archipelago_rs PR — upstream PR/issue: configurable/echoed Connect version (+ compressed-ws); removes the ER `4laric/archipelago_rs` fork. nex3 is a contact
- ER trap items — AP traps (summon bears/warp/freeze); mine Crowd Control mod; not started
- ER parriable deathlink — parry input negates an incoming deathlink (riposte=reflect back); SPEC-parriable-deathlink.md; needs the input/anim-read subsystem
- ER control randomizer — Skumnut special: start gimped, unlock controls (look-left/roll/double-jump) as AP items; client input-mask subsystem; spec-only, lowest pri
- ER boss-kill grace + fog audit — follow-up to boss-arena fix: sweep EMEVDs/MSBs for graces that appear on boss kill + for fog walls
- ER run trimmed mode — operational todo: haven't run trimmed mode in a while; do a regression run (trimmed measured 2498, DLC 573)
- ER num_regions chain spec — breadcrumb locks into 1..N chain for sphere gradient; stubs only
- ER Oops All Legacy Dungeons — chain legacy dungeons by grace-warp, delete open world; SPEC
- ER global scadutree blessing spec — DRAFT: fragments as game-wide power curve; crux=persist stored level
- ER pool builder — compose pool to a target size from a ranked ladder; junk_retention knob; SPEC
- ER check-trim spec — drop checks both bad-tier AND remote; somber far-cut wired; grace-proximity needs coords
- ER relevance-uplift spec — dlc_only: swap DLC filler for base-game juice; mirror of dlc_gear_curation; SPEC
- ER progressive consumables spec — progressive Tear/Seed/Scadutree/Revered Ash + glovewort bells; Roderika flags need extraction
- ER map-grant region tracker — region MAP item=unlock token + tracker; long-term
- ER in-game check indicators spec — enemy ghost-glow SpEffect + scarab audio cue; SPEC
- ER snowfast QoL — port thefifthmatt Mountaintops shortcuts; impl decision pending
- ER merchant bell-bearing logic — gate merchant shop checks behind their Bell Bearing; idea
- ER Moore shop rando idea — already randomized in gen; in-game access issue (Bell Bearing 2008900 skip=True kills route)
- ER QoL Patches shop + refresh — Patches Murkwater shop needs reload; general shop-refresh-on-unlock
- ER natural-key locks — use vanilla keys as region locks (Academy/Dectus/Rold/Haligtree/Irina); broad SPEC
- ER great_runes_present — force extra great runes into num_regions pool; default 0=no change
- ER Stormveil Rusty Key follow-ups — over-gate removed; Margit Shackle + Liurnia/Caelid Rusty Key rules pending
- ER bake warnings audit — triage all warnings in a full bake; backlog
- ER next deliverable: DLC enemy rando — unstrip DLC maps for enemies only; check fork config first
- ER ruins sweep PARKED — overworld ruins not discrete objects; revive via hand-curated list; DLC ruins ship
- ER lean-check curation wishlist — force-keep start fingers/early stones/chest weapons as lean checks; `chest` tag is the lever
- ER FogMod region-lock direction — replace play-region KICK w/ baked flag-gated fog WALL; +Liurnia Caves=1 lock over 8 dungeons
- ER fogwall vanilla-reuse verdict — KICK LOCKED IN as region-lock enforcement (2026-06-24). AEG099_720 self-collides but BAKER-placing it hung the load ×2: baker writes the MSB ref, NOT the asset's collision resources (map bundle). Real walls need Smithbox, not baker. Don't retry baker placement
- ER region-lock physical enforcement — fog-wall enforcement belongs in BAKER (EMEVD gated on region_open flag), not client
- ER boss attribution spec — SPEC for the (now-shipped) boss attribution; every check→one boss DefeatFlag

## Open bug notes / gotchas (reference)
- num_regions chain host reach — chain breadcrumb host parks locks in gated interiors → FillError; fix=reachability-filter cands
- num_regions pool strands Leyndell — rune_source=pool seals Altus→Leyndell; unblock=rune_source:regions
- num_regions pool+chain Limgrave — pool+chain rolled Limgrave → 2 start locks; fix excludes Limgrave from the chain roll (patch written, needs Windows run)
- _can_go_to warp / Radahn festival — _can_go_to checks geographic entrance not region-reach; fix=setflag festival in baker (flag UNCONFIRMED)
- Gelmir rebucket REVERSED — Gelmir is its OWN play_region 63001 (not Altus 63000); Volcano interior=16000; patch_apworld_region_ids_full_20260626.py un-rebuckets + fills area_ids
- play_region_id source — BonfireWarpParam.bonfireSubCategoryId = the KICK area_id per grace/region; REGION_ID_MAP.md + grace_region_map.tsv enumerate all 55 buckets / 422 graces
- ER key-item obtained flags — client grants goods but skips vanilla obtained-flag; Rold Medallion=400001; full 4000xx table
- ER DLC-only spec — SPEC-dlc-only.md; DLC check count 1207; access crux A transit vs B DLC-start
- ER rune-skip injectable room — create_items demand-drops small Golden Runes to fit region locks; fixes DLC-off overflow deadlock
- ER Ashes of War tiers — PvE tiers for all 105 transferable AoW in item_tiers; feeds injection

## ✅ Done / shipped
- ER runtime client port — pickup→grant→location check working end-to-end
- ER Godrick goal — ending_condition=godrick verified in-game
- ER num_regions — randomized region count for ~3-4hr runs; TESTED GREEN
- ER DLC mini-campaign — ending=messmer, 24 kept/20 sealed; rigorously tested green; Alaric's main DLC mode
- ER DLC auto-entry — Chapel latch→baked warp + Roundtable grace; worked completely smoothly first try
- ER region fusion — region_lock + grace bundle DONE & playtested (dlc_only Gravesite-grace-at-start was the m61 layer FAIL)
- ER slotdata areaLockFlags — fixed; region lock LIVE & confirmed working in-game
- ER grace rando — WORKING in-game; residual UX: empty check spots + reuse for boss sweep (see board k21/k22)
- ER dlc_only region-lock — gen-test passed
- ER bundle-lock graces — bundle locks now grant warp grace; closed out
- ER grace bundle boss-arena warp — grace_arena_exclude; closed (follow-up audit in backlog)
- ER shackle dlc_only inject — Margit's/Mohg's Shackle into UPLIFT_UNIQUE_CAPS; playtest-validated
- ER boss attribution impl — BossAttribution.cs + sweep emit; verified in-game
- ER auto_upgrade — implemented (live highest, normal/somber separate); verified in-game
- ER progressive stone bells — fully implemented apworld+client behind default-OFF
- ER progressive physick — validated; residual = duplicate tears in pool (low-sev, dedupe in create_items)
- ER physick grant crash — FIXED (client rebuilt); was killing ALL grants
- ER bell overflow rune — extra bell copies past max→Lord's Rune
- ER Torrent start grant — startItems goods 130 + flag 60100; tested green
- ER quick_start option — dlc_only grants 71 Lord's Runes at start; validated
- ER early leveling — skip-Melina flags 4680+951 shipped
- ER options consolidation — 84 fields→12 groups (presentation-only); done
- ER priority locations — important_locations priority-fill; done
- ER important_locations scope — num_regions/Enia-aware + per-region headroom; gen passed
- ER questline de-randomization — locks optional NPC quests at vanilla; tested green
- ER spell trim keep — 57 S/A spell tiers + HIGH_TIER_SPELLS keep; validated
- ER ammo filler guard — arrows/bolts excluded from filler→useful promotion
- ER filler_replacement option — swap filler for runes or stones+runes; count-neutral
- num_regions priority reach-prune — pre_fill max-state prune; 40/40 green
- num_regions fill overflow — 4 demotion patches for spine-seal "No more spots"
- soft_progression × bell gate — fix shipped
- base-hub start graces — grants Roundtable always + First Step if Limgrave free
- ER bake polish glow + double-grant — AP-check pickup glow + shop single-grant; fixed
- ER fix #7 volcano loop — fixpoint hardening of findLoops; fixed
- ER KeepDlcMaps bug — read enable_dlc raw not via BoolOptions; fix verified
- ?NpcName? deploy — always a build/deploy problem; L4396 precedence fix shipped
- ER DLC map flag — pieces 1000-1004 openEventFlagId 62080-84 are real per-area flags
- ER client save/load crash — auto_upgrade race + start-item timing; fixed via SEH wrap + settle-gate
- ER grace flag flush — gated flush on InventoryInstance()!=0
- ER start-items grant loop — fixed via pendingStartItems queue + persist once-per-save
- ER progressive tier persist — persist counter + highIndex
- ER client inventory removal — toasts are native popups (paced); removeFromInventory=direct edit
- ER superrepo merge — keep-ours + LF renorm + merge -s ours; runbook in repo
- ER notify outgoing sent — 'Sent X to Y' console line done; on-screen banner parked
- ER notify banner Task B — incoming banner abandoned (native ticker is UX); modal suppression confirmed
- ER AP-notify banner size — reuse ER native bottom-center event banner; Alaric likes its size
- ER Twin Maiden shop dlc_only — correctly VANILLA in dlc_only; not a bug
- ER defer grants in boss fight — built then removed (obsoleted by modal suppression); AOB/RVA preserved
- ER RLA crash + CrashFix.dll — PARKED; CrashFix.dll wired into deploy, wait for recurrence
