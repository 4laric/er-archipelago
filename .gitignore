# ER-Archipelago superrepo .gitignore
# The 6 4laric forks are SUBMODULES (tracked by gitlink, not by content here).
# Everything below is deliberately NOT committed to the superrepo.

# --- third-party upstream deps: re-clone manually, see SUPERREPO-SETUP.md ---
/Paramdex/
/yet-another-tab-control/

# --- non-repo vendored / large dirs ---
# Contains the copyrighted game exe + vanilla param dumps. NEVER commit (licensing).
/elden_ring_artifacts/
# AP generation test output.
/gen-test/
# Downloaded Nexus randomizer package.
/Elden Ring Randomizer-428-v0-11-4-1763103112 (2)/

# --- generated / build outputs ---
eldenring_*.apworld          # packaged apworld (rebuilt by build.ps1)
gensweep*
apconfig.json              # bake output (slot/seed/location_flags)
preflight_*.log
*.log
gendiag_*.txt              # per-gen compact diagnostic (dlcdiag.py output; raw .log already ignored)

# --- backups / archives ---
*.bak
*.tar.gz

# --- python cache (poptracker generator) ---
__pycache__/
*.pyc

# --- OS / editor
# --- boss-attribution dry-run analysis outputs (regenerate via boss_attribution_dryrun.py / tune_complement.py) ---
boss-attribution-dryrun.csv
check-nearest-grace.csv
smithing_stone_distribution.csv
