# ============================================================================
# ER ARCHIPELAGO -- MASTER OPTION TEMPLATE (all options, commented out).
# Auto-generated from worlds/eldenring/options.py 2026-06-19 by tools/gen_master_template.py.
# Each line shows the option at its DEFAULT value; uncomment + edit the ones you want.
# Valid values and a one-line description follow each option on the same line.
# NOTE: accessibility + progression_balancing are AP-core (not ER) -- listed at the end.
# ============================================================================

name: Player
description: ER Archipelago master template
game: EldenRing

EldenRing:
  # ending_condition: final_boss      # [final_boss / elden_beast / all_remembrances / all_bosses / capital / messmer / godrick] Ending Condition options
  # world_logic: region_lock          # [region_lock / region_bosses / region_lock_bosses / open_world] World Logic options
  # region_boss_percent: 50           # [1..100 (integer)] The % of bosses in a region to unlock the next
  # region_boss_type: false           # [false / true] Remove cave and catacombs type bosses from region bosses.
  # soft_logic: true                  # [false / true] Region Soft Logic
  # great_runes_required: 2           # [1..7 (integer)] How many great runes are required to enter Leyndell
  # great_runes_final_boss: 0         # [0..7 (integer)] How many great runes are required to access the final boss (the Erdtree:
  # great_runes_mountaintops: 0       # [0..7 (integer)] How many great runes are required to enter the Mountaintops of the Giants,
  # deathless_routing: false          # [false / true] Exclude the Volcano Manor abduction (a death-based shortcut from Raya Lucaria) from
  # graces_per_region: 3              # [0..12 (integer)] Region fusion (TODO #13): how many Sites of Grace to unlock per region when you receive
  # region_access: geographic         # [geographic / warp] How region-lock logic models REACHING a region (world_logic region_lock / region_bosses).
  # num_regions: 0                    # [0..9] random majors for a short capital run; add num_regions_order: spine for the old fixed first-N (region_count)
  # completion_scaling: off           # [off / flat / gentle / steep] Reshape Elden Ring's difficulty curve by seed-completion order rather than leave it vanilla.
  # completion_scaling_floor: 0       # [0..50 (integer)] Minimum scaling tier as a PERCENT of the baker's MaxTier (0 = earliest tier can stay 1;
  # random_start_region: off          # [off / overworld / any_major] Roll a random overworld region to start in instead of Limgrave / The First Step. The chosen
  # start_region_freebie: to_limgrave # [hub_only / to_limgrave] How much comes free with the rolled start region. The start region is always reachable with
  # royal_access: false               # [false / true] Keep Royal Capital graces accessable after it becomes ashen.
  # early_leveling: false             # [false / true] Grant the ability to Level Up at Sites of Grace from the start, skipping Melina's accord
  # extra_region_locks:               # [list of names] Opt-in granular region locks (region_lock / region_lock_bosses only). Each name splits a
  # enable_dlc: false                 # [false / true] Enable DLC
  # dlc_only: false                   # [false / true] Restrict the check pool to the Shadow of the Erdtree DLC only (base game kept for
  # quick_start: false                # [false / true] Quick Start (dlc_only): grant enough Lord's Runes at load-in to reach ~Runelevel 120
  # scadu_frontload: 8                # [0..50 (integer)] Bias this many TOTAL Scadutree Fragments into the earliest reachable locations
  # messmer_kindle: false             # [false / true] Messmer Kindle Shards
  # messmer_kindle_required: 5        # [2..15 (integer)] Messmer Kindle Shards required to access Enir Ilim.
  # messmer_kindle_max: 10            # [2..15 (integer)] How many Messmer Kindle Shards there are.
  # dlc_timing: off                   # [early / off / late] Guarantee that you don't need to enter the DLC until later in the run.
  # enemy_rando: false                # [false / true] Randomizes the enemies.
  # material_rando: true              # [false / true] Randomizes the indefinitely spawning materials.
  # death_link:                       # [?]
  # random_start: true                # [false / true] Randomizes the equipment characters begin with.
  # auto_equip: false                 # [false / true] Automatically equips any received armor or left/right weapons.
  # auto_upgrade: false               # [false / true] Automatically upgrades any received weapons to highest upgraded level.
  # progressive_stone_bells: false    # [false / true] Replace the 9 discrete Miner's Bell Bearings with 2 PROGRESSIVE items
  # progressive_bell_count: 15        # [5..40 (integer)] How many copies of EACH progressive stone bell (Smithing, Somber) to put in the pool
  # progressive_bell_early_count: 4   # [0..10 (integer)] How many copies of EACH progressive stone bell to FORCE into sphere-1 (no-item-reachable)
  # progressive_physick: false        # [false / true] Collapse the Flask of Wondrous Physick + the low-value Crystal tears into ONE
  # crafting_kit_option: early        # [randomize / early / do_not_randomize / start_with] Choose how the Crafting Kit is handled.
  # randomize_enia: true              # [false / true] Randomize the Roundtable / Enia (Finger Reader) slots -- Remembrance turn-ins and the
  # map_option: give                  # [randomize / give / do_not_randomize] Choose how maps are handled.
  # smithing_bell_bearing_option: progression_randomize # [randomize / progression_randomize / do_not_randomize] Choose how smithing stone bell bearings are handled.
  # merchant_bell_logic: off          # [off / logic_only] Gate merchant shop checks behind that merchant's Bell Bearing.
  # spell_shop_spells_only: false     # [false / true] Spell Shops only have spells.
  # early_legacy_dungeons: false      # [false / true] Early Legacy Dungeons are early
  # local_item_option: true           # [false / true] Only progression or useful items will show up in other players games.
  # exclude_local_item_only: ['Weapon', 'Armor', 'Accessory', 'AshofWar'] # [?] If LocalItemOnly is true then these item categories will show up in other players games.
  # important_locations: ['Remembrance', 'Seedtree', 'Map'] # [?] Prevent these location types from having an unimportant items.
  # exclude_locations:                # [?] Prevent these locations from having an important items.
  # excluded_location_behavior: forbid_useful # [allow_useful / forbid_useful / do_not_randomize] How to choose items for excluded locations in ER.
  # missable_location_behavior: forbid_useful # [allow_useful / forbid_useful / do_not_randomize] Which items can be placed in locations that can be permanently missed.
  # dungeon_sweep: none               # [none / minidungeons / all / bosses] Beating a dungeon's boss automatically sends every remaining check in that dungeon.
  # grace_sweep: off                  # [off / complement / full] Complementary trigger for Dungeon Sweep = **Bosses**: lighting a Site of Grace sweeps
  # no_weapon_requirements: false     # [false / true] Remove all stat requirements from weapons, ammo, and spells.
  # swap_multiboss: false             # [false / true] Enemy randomizer: swap bosses between multi-boss fights (mini-dungeon bosses only).
  # boss_runes_match: false           # [false / true] Enemy randomizer: a relocated boss drops the runes the original occupant of that
  # impolite_enemies: false           # [false / true] Enemy randomizer: make enemies less passive in groups (they aggro more readily).
  # disable_serpent_hunter_upgrade: false # [false / true] Disable upgrading the Serpent-Hunter greatspear (a niche base-randomizer balance
  # bell_physick_option: start_with   # [start_with / do_not_randomize / randomize] How to handle the Spirit Calling Bell and Flask of Wondrous Physick. Both are inert
  # torrent_start: auto               # [auto / on / off] Whether the Spectral Steed Whistle (Torrent) is granted at the start -- decoupled from
  # flask_upgrade_option: randomize   # [randomize / to_important / do_not_randomize] How to handle flask upgrades -- Golden Seeds (flask charges) and Sacred Tears (flask
  # blessing_option: randomize        # [randomize / to_important / do_not_randomize] How to handle Shadow Realm blessings -- Scadutree Fragments and Revered Spirit Ashes
  # location_pool: all                # [all / trimmed / lean] How many locations are randomized checks -- controls your footprint in a multiworld.
  # dlc_gear_curation: false          # [false / true] Curate the gear pool: drop bad base-game weapons/armor, and (with the DLC) bring in the best DLC gear.
  # junk_retention: 12                # [0..100 (integer)] Keep a fraction of deliberately-bad "comedy junk" as real checks instead of scrubbing
  # junk_retention_style: comedy      # [comedy / comedy_and_generic / uniform] What counts as sparable junk for Junk Retention.
  # filler_replacement: off           # [off / runes / stones_and_runes] Replace leftover junk / low-value filler in the item pool with worthwhile economy items.
  # no_spirit_ashes: false            # [false / true] Remove spirit summons from the run entirely. Every Spirit Ash / summon (generic and
  # progressive_flasks: false         # [false / true] Replace the discrete Golden Seeds and Sacred Tears with two PROGRESSIVE items
  # progressive_flask_early_count: 4  # [0..10 (integer)] How many copies of EACH progressive flask item (Golden Seed, Sacred Tear) to
  # progressive_glovewort_bells: false # [false / true] Replace the 6 discrete Glovewort Picker's Bell Bearings with two PROGRESSIVE items
  # pool_builder: false               # [false / true] Compose the item pool from an all-game priority ladder instead of inheriting whatever
  # pool_builder_dlc_gear: false      # [false / true] When pool_builder is on, also pull DLC S/A items into the all-game ladder -- S-tier DLC

  # ---- AP-core options ----
  # accessibility: full          # full / minimal / none
  # progression_balancing: 50    # 0..99 (default 50)
